Code: C Программа: Code::Blocks
#include <math.h>
#include <stdio.h>
#include <locale.h>
float deposit(float, float, float);
void result(float money)
{
  printf("Income sostavil: %f", money);
}
int main()
{
    float x, y, z, w;
    printf("Vvedite sum:");
    scanf("%f", &x);
    printf("Vvedite procent:");
    scanf("%f", &y);
    printf("Vvedie time:");
    scanf("%f", &z);
    w=deposit(x, y, z);
    result(w);
    return 0;
}
float deposit(float sum, float procent, float time)
{
    float income;
    income=sum*(procent/100)*(time/365);
    return(income);
}
